const nav=[['overview','Overview'],['personas','Personas'],['tasks','Tasks'],['runs','Runs'],['settings','Settings']];
export function Chrome({view,go,children}){const currentPage=view==='evidence'||view==='runs'?'runs':view;return <div className="app-frame"><header className="topbar"><button type="button" className="wordmark" onClick={()=>go('landing')}>PERSONA LAB <span>rough draft</span></button><div className="project-chip">Acme Market / Grocery flow</div><div className="disclosure">Synthetic examples · hypotheses only · not human research</div></header><div className="app-body"><aside className="sidebar"><nav className="workspace-nav" aria-label="Workspace"><p>Workspace</p>{nav.map(([id,label])=><button type="button" key={id} className={currentPage===id?'active':''} aria-current={currentPage===id?'page':undefined} onClick={()=>go(id)}>{label}</button>)}</nav><div className="side-note">One platform<br/>6 mapped pages<br/>3 personas</div></aside><main className="content">{children}</main></div></div>}
